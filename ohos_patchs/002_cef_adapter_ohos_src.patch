From 6af9c9d30a74ff7bd78ec9eea8aac253376218c2 Mon Sep 17 00:00:00 2001
From: laixiaohai <xiaohai.lai@haitaichina.com>
Date: Tue, 31 Mar 2026 17:34:17 +0800
Subject: [PATCH] add cef_adapter_ohos_src

Signed-off-by: laixiaohai <xiaohai.lai@haitaichina.com>
Change-Id: I01df644499d4e28919acf35f7d75f27f974d0c13
---
 .../src/partition_alloc/BUILD.gn              |   2 +-
 .../constrained_window_views.cc               |   9 +-
 .../internal/common/download_file_impl.cc     |   9 ++
 ohos/adapter/web_entry/BUILD.gn               |   4 +
 ohos/adapter/web_entry/cef/cef_loader.cc      | 105 ++++++++++++++++++
 ohos/adapter/web_entry/cef/cef_loader.h       |  19 ++++
 ohos/adapter/web_entry/web_entry.cc           |  16 ++-
 ohos/adapter/web_entry/web_loader.cc          |   3 +
 ohos/adapter/web_entry/web_loader.h           |   7 +-
 ohos/adapter/web_entry/web_loader_factory.cc  |  17 ++-
 ohos/app/ohos_hap/AppScope/app.json5          |   2 +-
 .../resources/base/element/string.json        |   2 +-
 .../main/resources/base/element/string.json   |   2 +-
 .../main/resources/en_US/element/string.json  |   2 +-
 .../main/resources/zh_CN/element/string.json  |   2 +-
 ohos/app/ohos_hap/oh-package.json5            |   2 +-
 .../src/main/ets/ability/WebBaseAbility.ets   |   3 +-
 .../src/main/ets/adapter/AppWindowAdapter.ets |   2 +-
 .../main/ets/components/WebEmbeddedWindow.ets |   2 +-
 .../ets/components/WebNodeHandleWindow.ets    |   2 +-
 .../src/main/ets/components/WebWindow.ets     |   2 +-
 .../web_engine/src/main/ets/utils/KVStore.ets |   2 +-
 ohos/build/build.sh                           |  13 ++-
 23 files changed, 205 insertions(+), 24 deletions(-)
 create mode 100644 ohos/adapter/web_entry/cef/cef_loader.cc
 create mode 100644 ohos/adapter/web_entry/cef/cef_loader.h

diff --git a/src/base/allocator/partition_allocator/src/partition_alloc/BUILD.gn b/src/base/allocator/partition_allocator/src/partition_alloc/BUILD.gn
index 4c70d7c0ed..899ab3fefc 100644
--- a/src/base/allocator/partition_allocator/src/partition_alloc/BUILD.gn
+++ b/src/base/allocator/partition_allocator/src/partition_alloc/BUILD.gn
@@ -925,7 +925,7 @@ if (is_clang_or_gcc) {
     }
 
     if (!use_partition_alloc_as_malloc) {
-      if (is_android) {
+      if (is_android || is_ohos) {
         shim_sources += [
           "shim/allocator_shim_default_dispatch_to_linker_wrapped_symbols.cc",
         ]
diff --git a/src/components/constrained_window/constrained_window_views.cc b/src/components/constrained_window/constrained_window_views.cc
index 136c93d146..4de4acfb45 100644
--- a/src/components/constrained_window/constrained_window_views.cc
+++ b/src/components/constrained_window/constrained_window_views.cc
@@ -336,9 +336,12 @@ views::Widget* CreateBrowserModalDialogViews(views::DialogDelegate* dialog,
       parent ? CurrentBrowserModalClient()->GetDialogHostView(parent)
              : gfx::NativeView();
   // Use with CEF windowless rendering.
+  ModalDialogHost* host =
+      CurrentBrowserModalClient()->GetModalDialogHost(parent);
   gfx::AcceleratedWidget parent_widget =
-      parent ? CurrentBrowserModalClient()->GetModalDialogHost(parent)->
-          GetAcceleratedWidget() : gfx::kNullAcceleratedWidget;
+      parent
+          ? (host ? host->GetAcceleratedWidget() : gfx::kNullAcceleratedWidget)
+          : gfx::kNullAcceleratedWidget;
   views::Widget* widget = views::DialogDelegate::CreateDialogWidget(
       dialog, gfx::NativeWindow(), parent_view, parent_widget);
   widget->SetNativeWindowProperty(
@@ -356,8 +359,6 @@ views::Widget* CreateBrowserModalDialogViews(views::DialogDelegate* dialog,
   if (!requires_positioning)
     return widget;
 
-  ModalDialogHost* host =
-      CurrentBrowserModalClient()->GetModalDialogHost(parent);
   if (host) {
     DCHECK_EQ(parent_view, host->GetHostView());
     std::unique_ptr<ModalDialogHostObserver> observer =
diff --git a/src/components/download/internal/common/download_file_impl.cc b/src/components/download/internal/common/download_file_impl.cc
index 6d48cd769c..f1cf9b4d48 100644
--- a/src/components/download/internal/common/download_file_impl.cc
+++ b/src/components/download/internal/common/download_file_impl.cc
@@ -23,6 +23,7 @@
 #include "base/trace_event/trace_event.h"
 #include "base/values.h"
 #include "build/build_config.h"
+#include "cef/libcef/features/features.h"
 #include "components/download/internal/common/parallel_download_utils.h"
 #include "components/download/public/common/download_create_info.h"
 #include "components/download/public/common/download_destination_observer.h"
@@ -415,8 +416,16 @@ void DownloadFileImpl::RenameAndUniquify(const base::FilePath& full_path,
     return;
   }
 #endif  // BUILDFLAG(IS_ANDROID)
+#if BUILDFLAG(ENABLE_CEF) && BUILDFLAG(IS_OHOS)
+  // If there's already a file on disk that conflicts with |new_path|, try to
+  // create a unique file by appending a uniquifier will be failed, due to
+  // file-manager set permissions only for full_path on ohos platform for CEF.
+  std::unique_ptr<RenameParameters> parameters(new RenameParameters(
+      ANNOTATE_WITH_SOURCE_INFORMATION, full_path, std::move(callback)));
+#else
   std::unique_ptr<RenameParameters> parameters(
       new RenameParameters(UNIQUIFY, full_path, std::move(callback)));
+#endif  // BUILDFLAG(ENABLE_CEF) && BUILDFLAG(IS_OHOS)
   RenameWithRetryInternal(std::move(parameters));
 }
 
diff --git a/src/ohos/adapter/web_entry/BUILD.gn b/src/ohos/adapter/web_entry/BUILD.gn
index 176523d86f..550a7adfbe 100644
--- a/src/ohos/adapter/web_entry/BUILD.gn
+++ b/src/ohos/adapter/web_entry/BUILD.gn
@@ -7,6 +7,8 @@ import("//build/config/chrome_build.gni")
 
 component("web_entry") {
     sources = [
+      "cef/cef_loader.cc",
+      "cef/cef_loader.h",
       "chrome/chrome_loader.cc",
       "chrome/chrome_loader.h",
       "permission_constants.h",
@@ -25,6 +27,8 @@ component("web_entry") {
       defines = [ "USE_LOADER_TARGET=0" ]
     } else if (ohos_build_target == "chromium") {
       defines = [ "USE_LOADER_TARGET=1" ]
+    } else if (ohos_build_target == "cef") {
+      defines = [ "USE_LOADER_TARGET=2" ]
     } else {
       assert(true, "$ohos_build_target target builds are not supported on ohos")
     }
diff --git a/src/ohos/adapter/web_entry/cef/cef_loader.cc b/src/ohos/adapter/web_entry/cef/cef_loader.cc
new file mode 100644
index 0000000000..79c017dc06
--- /dev/null
+++ b/src/ohos/adapter/web_entry/cef/cef_loader.cc
@@ -0,0 +1,105 @@
+// Copyright (c) 2024 Huawei Device Co., Ltd. All rights reserved.
+// Use of this source code is governed by a BSD-style license that can be
+// found in the LICENSE file.
+
+#include "ohos/adapter/web_entry/cef/cef_loader.h"
+
+#include <accesstoken/ability_access_control.h>
+
+#include "ohos/adapter/common/logging.h"
+#include "ohos/adapter/common/shared_library.h"
+#include "ohos/adapter/dev_config/dev_config.h"
+#include "ohos/adapter/device_info/device_info.h"
+#include "ohos/adapter/web_entry/permission_constants.h"
+
+namespace ohos::adapter::web_entry {
+namespace {
+__attribute__((no_sanitize("cfi", "cfi-icall")))
+bool CheckAdvSecMode() {
+  uint32_t mode = -1;
+  common::SharedLibrary shared_library_("/system/lib64/platformsdk/",
+                                        "dsmm_innersdk.z");
+  auto fn = shared_library_.GetFunction<int(const char*, uint32_t, const char*,
+                                            uint32_t, uint32_t*)>(
+      "AdvancedSecurityModeGetStateByFeature");
+  if (fn != nullptr) {
+    // returns 0 if success or errcode
+    int32_t ret = fn("default", 7, "default", 7, &mode); // len:7
+    if (ret == 0 && mode == 1) {
+      return true;
+    }
+  }
+  return false;
+}
+
+bool ShouldDisableJit() {
+  if (CheckAdvSecMode()) {
+    return true;
+  }
+  
+  if (!OH_AT_CheckSelfPermission(
+      PermissionConstants::ALLOW_WRITABLE_CODE_MEMORY)) {
+    return true;
+  }
+  
+  return false;
+}
+
+}  // namespace
+
+std::string CefLoader::GetEntryPoint(int process_type) {
+  std::string entry_point;
+  switch (process_type) {
+    case ProcessType::kMainProcess:
+    case ProcessType::kRenderProcess:
+    case ProcessType::kGpuProcess:
+      entry_point = "CefMain";
+      break;
+    case ProcessType::kCrashpadHandler:
+      entry_point = "CrashpadHandlerMain";
+      break;
+    default:
+      LOGE("GetEntryPoint nothing to do, unknown process type: %{public}d",
+           process_type);
+      break;
+  }
+  return entry_point;
+}
+
+const std::vector<std::string> CefLoader::GetEntryArgs() {
+  std::vector<std::string> args = {
+      "cef",
+      "--use-gl=egl",
+      "--enable-features=UseOzonePlatform",
+      "--ozone-platform=ohos",
+      "--enable-logging",
+      "--ozone-dump-file=/data/storage/el2/base/cache/",
+      "--no-zygote",
+      "--user-data-dir=/data/storage/el2/base/files/",
+      "--disable-gpu-watchdog",
+      "--force-renderer-accessibility=basic",
+      "--enable-media-stream=true",
+      "--enable-speech-input=true",
+      "--enable-print-preview",
+      "--lang=zh-CN",
+  };
+  ohos::adapter::device_info::DeviceType device_type =
+      ohos::adapter::device_info::DeviceInfo::GetInstance().GetDeviceType();
+  // pad not support gpu process
+  if (device_type == ohos::adapter::device_info::DeviceType::_TABLET) {
+    if (ohos::adapter::device_info::DeviceInfo::SdkApi() <
+        ohos::adapter::device_info::SDK_VERSION_19) {
+          args.emplace_back("--js-flags=--jitless");
+    }
+    args.push_back("--in-process-gpu");
+  }
+  if (ShouldDisableJit()) {
+    args.emplace_back("--js-flags=--jitless");
+  }
+  for (const auto& command : GetDevCommandLines()) {
+    args.emplace_back(command);
+  }
+  return args;
+}
+
+}  // namespace ohos::adapter::web_entry
diff --git a/src/ohos/adapter/web_entry/cef/cef_loader.h b/src/ohos/adapter/web_entry/cef/cef_loader.h
new file mode 100644
index 0000000000..6a7cfa2044
--- /dev/null
+++ b/src/ohos/adapter/web_entry/cef/cef_loader.h
@@ -0,0 +1,19 @@
+// Copyright (c) 2024 Huawei Device Co., Ltd. All rights reserved.
+// Use of this source code is governed by a BSD-style license that can be
+// found in the LICENSE file.
+
+#include "ohos/adapter/web_entry/web_loader.h"
+
+namespace ohos::adapter::web_entry {
+
+class CefLoader : public BasicLoader {
+ public:
+  CefLoader(const std::string& dir, const std::string& name)
+      : BasicLoader(dir, name) {}
+  CefLoader(const std::string& lib_path) : BasicLoader(lib_path) {}
+
+  std::string GetEntryPoint(int process_type) override;
+  const std::vector<std::string> GetEntryArgs() override;
+};
+
+}  // namespace ohos::adapter::web_entry
diff --git a/src/ohos/adapter/web_entry/web_entry.cc b/src/ohos/adapter/web_entry/web_entry.cc
index cf6a2b7b16..7c28909f2c 100644
--- a/src/ohos/adapter/web_entry/web_entry.cc
+++ b/src/ohos/adapter/web_entry/web_entry.cc
@@ -7,6 +7,7 @@
 
 #include <atomic>
 #include <chrono>
+#include <filesystem>
 #include <thread>
 
 #include "ohos/adapter/aki_hook/aki_hook.h"
@@ -63,7 +64,20 @@ int RunOtherProcessType(int process_type) {
   std::vector<std::string> commands;
   multiprocess::AppSpawnCommunication::RequestCommandline(commands);
   std::vector<const char*> cstrs;
-  auto loader = BasicLoader::Create(USE_LOADER_TARGET);
+  std::string subprocess_path_name = "";
+  if (int(USE_LOADER_TARGET) == int(WebLoaderType::kCef)) {
+    const std::string sso_flag = ".so";
+    if (!commands.empty() && !commands[0].empty()) {
+      std::filesystem::path file_path(commands[0].data());
+      if (file_path.extension().string() == sso_flag) {
+        subprocess_path_name = commands[0].data();
+        LOGI("cef --browser-subprocess-path= %{public}s",
+             subprocess_path_name.c_str());
+      }
+    }
+  }
+  auto loader =
+      BasicLoader::Create(USE_LOADER_TARGET, subprocess_path_name.c_str());
   for (size_t i = 0; i < commands.size(); ++i) {
     if (commands[i].find("--type") != std::string::npos) {
       auto pos = commands[i].find_last_of("=");
diff --git a/src/ohos/adapter/web_entry/web_loader.cc b/src/ohos/adapter/web_entry/web_loader.cc
index 9087bc3678..2c44023260 100644
--- a/src/ohos/adapter/web_entry/web_loader.cc
+++ b/src/ohos/adapter/web_entry/web_loader.cc
@@ -10,6 +10,9 @@ namespace ohos::adapter::web_entry {
 BasicLoader::BasicLoader(const std::string& dir, const std::string& name)
     : shared_library_(dir, name) {}
 
+BasicLoader::BasicLoader(const std::string& lib_path)
+    : shared_library_(lib_path) {}
+
 bool BasicLoader::Initialize() { return true; }
 
 __attribute__((no_sanitize("cfi", "cfi-icall")))
diff --git a/src/ohos/adapter/web_entry/web_loader.h b/src/ohos/adapter/web_entry/web_loader.h
index d7b8b7409b..63e7a6e404 100644
--- a/src/ohos/adapter/web_entry/web_loader.h
+++ b/src/ohos/adapter/web_entry/web_loader.h
@@ -20,6 +20,7 @@ using OnLoaderCallback = std::function<void(int error_code)>;
 enum WebLoaderType {
   kContent_shell = 0,
   kChrome,
+  kCef,
 };
 
 class BasicLoader {
@@ -27,12 +28,14 @@ class BasicLoader {
   BasicLoader(const BasicLoader&) = delete;
   BasicLoader& operator=(const BasicLoader&) = delete;
   BasicLoader(const std::string& dir, const std::string& name);
+  BasicLoader(const std::string& lib_path);
 
   virtual ~BasicLoader() {}
 
   // A factory method that can create any of the derived types of BasicLoader
-  // (ContentShellLoader and ChromeMainLoader ).
-  static BasicLoader* Create(int loaderType);
+  // (ContentShellLoader and ChromeMainLoader and CefLoader).
+  // Maybe CefLoader will add --browser-subprocess-path, input path here
+  static BasicLoader* Create(int loaderType, std::string subprocess_path = "");
 
   bool Initialize();
 
diff --git a/src/ohos/adapter/web_entry/web_loader_factory.cc b/src/ohos/adapter/web_entry/web_loader_factory.cc
index 8208769f58..1f4dd98f01 100644
--- a/src/ohos/adapter/web_entry/web_loader_factory.cc
+++ b/src/ohos/adapter/web_entry/web_loader_factory.cc
@@ -5,6 +5,7 @@
 #include <memory>
 
 #include "ohos/adapter/common/logging.h"
+#include "ohos/adapter/web_entry/cef/cef_loader.h"
 #include "ohos/adapter/web_entry/chrome/chrome_loader.h"
 #include "ohos/adapter/web_entry/shell/content_shell_loader.h"
 #include "ohos/adapter/web_entry/web_loader.h"
@@ -16,8 +17,10 @@ namespace {
 static std::unique_ptr<BasicLoader> current_default_loader = nullptr;
 const std::string kShellEntry = "content_shell_web";
 const std::string kChromeEntry = "chrome_main_web";
+const std::string kCefEntry = "cefsimple";
 
-std::unique_ptr<BasicLoader> GetCurrentDefaultLoader(int loaderType) {
+std::unique_ptr<BasicLoader> GetCurrentDefaultLoader(int loaderType,
+                                                     std::string subprocess_path) {
   std::unique_ptr<BasicLoader> loader;
   switch (loaderType) {
     case WebLoaderType::kContent_shell:
@@ -26,6 +29,13 @@ std::unique_ptr<BasicLoader> GetCurrentDefaultLoader(int loaderType) {
     case WebLoaderType::kChrome:
       loader = std::make_unique<ChromeMainLoader>("", kChromeEntry);
       break;
+    case WebLoaderType::kCef:
+      if (subprocess_path.empty()) {
+        loader = std::make_unique<CefLoader>("", kCefEntry);
+      } else {
+        loader = std::make_unique<CefLoader>(subprocess_path);
+      }
+      break;
     default:
       LOGE("create web loader error unknown loaderType: %{public}d", loaderType);
       break;
@@ -36,9 +46,10 @@ std::unique_ptr<BasicLoader> GetCurrentDefaultLoader(int loaderType) {
 }  // namespace
 
 // static
-BasicLoader* BasicLoader::Create(int loaderType) {
+BasicLoader* BasicLoader::Create(int loaderType, std::string subprocess_path) {
   if (!current_default_loader) {
-    current_default_loader = GetCurrentDefaultLoader(loaderType);
+    current_default_loader =
+        GetCurrentDefaultLoader(loaderType, subprocess_path);
   }
   return current_default_loader.get();
 }
diff --git a/src/ohos/app/ohos_hap/AppScope/app.json5 b/src/ohos/app/ohos_hap/AppScope/app.json5
index 0618d07d48..42705f6173 100644
--- a/src/ohos/app/ohos_hap/AppScope/app.json5
+++ b/src/ohos/app/ohos_hap/AppScope/app.json5
@@ -1,6 +1,6 @@
 {
   "app": {
-    "bundleName": "com.huawei.ohos_chromium",
+    "bundleName": "com.huawei.ohos_cef",
     "vendor": "example",
     "versionCode": 1000001,
     "versionName": "1.0.1",
diff --git a/src/ohos/app/ohos_hap/AppScope/resources/base/element/string.json b/src/ohos/app/ohos_hap/AppScope/resources/base/element/string.json
index ed6d17651a..1d48845df4 100644
--- a/src/ohos/app/ohos_hap/AppScope/resources/base/element/string.json
+++ b/src/ohos/app/ohos_hap/AppScope/resources/base/element/string.json
@@ -2,7 +2,7 @@
   "string": [
     {
       "name": "app_name",
-      "value": "ohos_chromium"
+      "value": "CEF"
     }
   ]
 }
diff --git a/src/ohos/app/ohos_hap/chromium/src/main/resources/base/element/string.json b/src/ohos/app/ohos_hap/chromium/src/main/resources/base/element/string.json
index e401617523..3c94d3e6eb 100644
--- a/src/ohos/app/ohos_hap/chromium/src/main/resources/base/element/string.json
+++ b/src/ohos/app/ohos_hap/chromium/src/main/resources/base/element/string.json
@@ -10,7 +10,7 @@
     },
     {
       "name": "EntryAbility_label",
-      "value": "Chromium"
+      "value": "CEF"
     },
     {
       "name": "BrowserAbility_desc",
diff --git a/src/ohos/app/ohos_hap/chromium/src/main/resources/en_US/element/string.json b/src/ohos/app/ohos_hap/chromium/src/main/resources/en_US/element/string.json
index c62f2dfdd4..19453ed121 100644
--- a/src/ohos/app/ohos_hap/chromium/src/main/resources/en_US/element/string.json
+++ b/src/ohos/app/ohos_hap/chromium/src/main/resources/en_US/element/string.json
@@ -10,7 +10,7 @@
     },
     {
       "name": "EntryAbility_label",
-      "value": "Chromium"
+      "value": "CEF"
     },
     {
       "name": "OpenNewWindow_label",
diff --git a/src/ohos/app/ohos_hap/chromium/src/main/resources/zh_CN/element/string.json b/src/ohos/app/ohos_hap/chromium/src/main/resources/zh_CN/element/string.json
index bc3615452d..8d4da7c323 100644
--- a/src/ohos/app/ohos_hap/chromium/src/main/resources/zh_CN/element/string.json
+++ b/src/ohos/app/ohos_hap/chromium/src/main/resources/zh_CN/element/string.json
@@ -10,7 +10,7 @@
     },
     {
       "name": "EntryAbility_label",
-      "value": "Chromium"
+      "value": "CEF"
     },
     {
       "name": "OpenNewWindow_label",
diff --git a/src/ohos/app/ohos_hap/oh-package.json5 b/src/ohos/app/ohos_hap/oh-package.json5
index f131d9efab..f75e44ac53 100644
--- a/src/ohos/app/ohos_hap/oh-package.json5
+++ b/src/ohos/app/ohos_hap/oh-package.json5
@@ -5,7 +5,7 @@
     "@ohos/hypium": "1.0.6"
   },
   "author": "",
-  "name": "ohos_chromium",
+  "name": "ohos_cef",
   "description": "Please describe the basic information.",
   "main": "",
   "version": "1.0.0",
diff --git a/src/ohos/app/ohos_hap/web_engine/src/main/ets/ability/WebBaseAbility.ets b/src/ohos/app/ohos_hap/web_engine/src/main/ets/ability/WebBaseAbility.ets
index 4f1fd1591a..5fa49502e9 100644
--- a/src/ohos/app/ohos_hap/web_engine/src/main/ets/ability/WebBaseAbility.ets
+++ b/src/ohos/app/ohos_hap/web_engine/src/main/ets/ability/WebBaseAbility.ets
@@ -24,7 +24,8 @@ export class WebBaseAbility extends UIAbility implements WebProxy {
   protected windowStage: window.WindowStage | undefined = undefined;
   protected nativeContext: NativeContext =
     JsBindingUtils.getNativeContext(ContextType.kMainProcess);
-  protected hideTitleBar: boolean = true;
+  // cef: Need to display title bar
+  protected hideTitleBar: boolean = false;
   protected originWindowId: number = -1;
   protected useDarkMode: boolean = false;
   protected isStateless: boolean = false;
diff --git a/src/ohos/app/ohos_hap/web_engine/src/main/ets/adapter/AppWindowAdapter.ets b/src/ohos/app/ohos_hap/web_engine/src/main/ets/adapter/AppWindowAdapter.ets
index 68d09d4867..0f8da97066 100644
--- a/src/ohos/app/ohos_hap/web_engine/src/main/ets/adapter/AppWindowAdapter.ets
+++ b/src/ohos/app/ohos_hap/web_engine/src/main/ets/adapter/AppWindowAdapter.ets
@@ -28,7 +28,7 @@ const TAG: string = 'AppWindow';
 @injectable()
 export class AppWindowAdapter extends BaseAdapter {
   private abilityManager: AbilityManager;
-  private bundleName: string = 'com.huawei.ohos_chromium';
+  private bundleName: string = 'com.huawei.ohos_cef';
   private bundleFlags = bundleManager.BundleFlag.GET_BUNDLE_INFO_WITH_APPLICATION |
                         bundleManager.BundleFlag.GET_BUNDLE_INFO_WITH_METADATA;
   private queryWindowNum: number = 10;                      
diff --git a/src/ohos/app/ohos_hap/web_engine/src/main/ets/components/WebEmbeddedWindow.ets b/src/ohos/app/ohos_hap/web_engine/src/main/ets/components/WebEmbeddedWindow.ets
index d74caeb6ae..4ef34aa169 100644
--- a/src/ohos/app/ohos_hap/web_engine/src/main/ets/components/WebEmbeddedWindow.ets
+++ b/src/ohos/app/ohos_hap/web_engine/src/main/ets/components/WebEmbeddedWindow.ets
@@ -152,7 +152,7 @@ export struct WebEmbeddedWindow {
   }
 
   private buildArgs(): string[] {
-    let default_uri = '';
+    let default_uri = '--url=https://ams.haitaibrowser.com:8000/ohcef/ohindex.html';
     if (this.startUri.length !== 0) {
       default_uri = this.startUri;
     }
diff --git a/src/ohos/app/ohos_hap/web_engine/src/main/ets/components/WebNodeHandleWindow.ets b/src/ohos/app/ohos_hap/web_engine/src/main/ets/components/WebNodeHandleWindow.ets
index d908f9fe3f..1e2dac28f9 100644
--- a/src/ohos/app/ohos_hap/web_engine/src/main/ets/components/WebNodeHandleWindow.ets
+++ b/src/ohos/app/ohos_hap/web_engine/src/main/ets/components/WebNodeHandleWindow.ets
@@ -99,7 +99,7 @@ export struct WebNodeHandleWindow {
   }
  
   private buildArgs(): string[] {
-    let default_uri = '';
+    let default_uri = '--url=https://ams.haitaibrowser.com:8000/ohcef/ohindex.html';
     if (this.startUri.length !== 0) {
       default_uri = this.startUri;
     }
diff --git a/src/ohos/app/ohos_hap/web_engine/src/main/ets/components/WebWindow.ets b/src/ohos/app/ohos_hap/web_engine/src/main/ets/components/WebWindow.ets
index 676d36168d..b11a5c24d6 100644
--- a/src/ohos/app/ohos_hap/web_engine/src/main/ets/components/WebWindow.ets
+++ b/src/ohos/app/ohos_hap/web_engine/src/main/ets/components/WebWindow.ets
@@ -161,7 +161,7 @@ export struct WebWindow {
   }
 
   private buildArgs(): string[] {
-    let default_uri = '';
+    let default_uri = '--url=https://ams.haitaibrowser.com:8000/ohcef/ohindex.html';
     if (this.startUri.length !== 0) {
       default_uri = this.startUri;
     }
diff --git a/src/ohos/app/ohos_hap/web_engine/src/main/ets/utils/KVStore.ets b/src/ohos/app/ohos_hap/web_engine/src/main/ets/utils/KVStore.ets
index 5a2fbb1921..83bc01b87f 100644
--- a/src/ohos/app/ohos_hap/web_engine/src/main/ets/utils/KVStore.ets
+++ b/src/ohos/app/ohos_hap/web_engine/src/main/ets/utils/KVStore.ets
@@ -124,7 +124,7 @@ class KVManager {
     try {
       const kvManagerConfig: distributedKVStore.KVManagerConfig = {
         context: ctx,
-        bundleName: "com.huawei.ohos_chromium"
+        bundleName: "com.huawei.ohos_cef"
       };
       this.kvManager = distributedKVStore.createKVManager(kvManagerConfig);
       if (this.kvManager == undefined) {
diff --git a/src/ohos/build/build.sh b/src/ohos/build/build.sh
index 75017131b2..b9dc2dd86f 100755
--- a/src/ohos/build/build.sh
+++ b/src/ohos/build/build.sh
@@ -20,6 +20,7 @@ ROOT_DIR="${CUR_DIR%/src*}""/src"
 # Global variables.
 BUILD_TARGET_CONTENT_SHELL="content_shell_web"
 BUILD_TARGET_CHROME="chrome_main_web"
+BUILD_TARGET_CEF="libcef cefsimple cefclient ceftests"
 TEXT_BOLD="\033[1m"
 TEXT_NORMAL="\033[0m"
 
@@ -59,6 +60,10 @@ buildargs="
   llvm_ohos_mainline=true
   rustc_version=\"4a0969e06dbeaaa43914d2d00b2e843d49aa3886-1-llvmorg-21-init-11777-gfd3fecfc\"
   rust_sysroot_absolute=\"//ohos_sdk/rust-toolchain\"
+  
+# cef args
+  use_partition_alloc_as_malloc=false
+  enable_backup_ref_ptr_support=false
   "
 #Add build args end
 
@@ -76,7 +81,7 @@ buildarg_cpu="target_cpu=\"arm64\""
 buildarg_musl="use_musl=true"
 build_dir="out/musl_64/"
 build_product_name="product_name=\"all\""
-build_target="${BUILD_TARGET_CHROME}"
+build_target="${BUILD_TARGET_CEF}"
 build_output=""
 build_asan=0
 build_isolated_level=0
@@ -166,6 +171,12 @@ case "${build_target}" in
     ohos_build_target=\"chromium\"
     "
     ;;
+  "c"|"${BUILD_TARGET_CEF}")
+    build_target="${BUILD_TARGET_CEF}"
+    buildargs="${buildargs}safe_browsing_mode=1
+    ohos_build_target=\"cef\"
+    "
+    ;;
   *)
     echo "build_target: ${build_target}"
     ;;
-- 
2.34.1

